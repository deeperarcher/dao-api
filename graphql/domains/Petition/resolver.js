import { get as getIntakeForms } from '../IntakeForm/services';

export default {
  // Petition: {
  //   incidents: async ({ intakeForms }) => await getIntakeForms().filter(({ incidents}) => !!incidents.find(({petitionNumber}) => petitionNumber === petition.petitionNumber)),
  // },
};
